<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  {% load static %}
  <title>{% block title %}Jud in Foco{% endblock %}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <meta name="description" content="app jud in foco">
  <meta name="author" content="humbertho.mattar">

  <!-- Bootstrap core CSS -->
  <link href="{% static 'vendor/bootstrap/css/bootstrap.min.css' %}" rel="stylesheet">

  <!-- Custom styles for this template -->
  <link href="{% static 'css/simple-sidebar.css' %}" rel="stylesheet">
</head>

<body>
  <div class="d-flex" id="wrapper">
    <!-- Sidebar -->
    <div class="bg-light border-right" id="sidebar-wrapper">
      <div class="sidebar-heading">
        <center>
          <a href="{%url 'home' %}">
            <img src="{% static 'img/jud-in-foco.png' %}" alt="Jud in foco">
            <font color="#075280">
              <p><strong> Jud <i>in</i> Foco </strong></p>
            </font>
        </a>
        </center>
      </div>
      <div class="list-group list-group-flush">
          <a href="{%url 'home' %}" class="list-group-item list-group-item-action bg-light">Home</a> 
        
          <a class="btn collapsed list-group-item list-group-item-action bg-light" data-toggle="collapse" href="#collapseGE" role="button" aria-expanded="true" aria-controls="collapseGE">
              Guia de Execução
            </a>  
            <div class="collapse" id="collapseGE">
                <a href="#" class="list-group-item list-group-item-action bg-light">> Pesquisar</a>
                  <a href="#" class="list-group-item list-group-item-action bg-light">> Nova</a>
                  <a href="#" class="list-group-item list-group-item-action bg-light">> Distribuir</a>
            </div>

          <a class="btn collapsed list-group-item list-group-item-action bg-light" 
            data-toggle="collapse" href="#collapseAdministracao" role="button" 
            aria-expanded="true" aria-controls="collapseAdministracao">
              Administração
            </a>  
            <div class="collapse" id="collapseAdministracao">
                  <a href="{%url 'users' %}" class="list-group-item list-group-item-action bg-light">> Usuários</a>
                  <a href="#" class="list-group-item list-group-item-action bg-light">> Configurações</a>
            </div>
 
      </div>
    </div>
    <!-- /#sidebar-wrapper -->

    <!-- Page Content -->
    <div id="page-content-wrapper">

      <nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom">
        <button class="btn btn-primary" id="menu-toggle">
          Recolher
        </button>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
          aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav ml-auto mt-2 mt-lg-0">
            <a class="nav-link" href="#">
              {% if user.is_authenticated %}
              Olá {{ user.username }}!
              {% else %}
              <meta http-equiv="REFRESH" content="0;url={%url 'login' %}">
              <p>Você não está autenticada.</p>
              {% endif %}
            </a>
            </li>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
                aria-haspopup="true" aria-expanded="true">
                Ações
              </a>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                <a class="dropdown-item disabled" href="#">menu</a>
                <a class="dropdown-item disabled" href="#">menu</a>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="{% url 'logout' %}">Sair</a>
              </div>
            </li>
          </ul>
        </div>
      </nav>

      <div class="container-fluid">
        <main>
          {% block content %}


          {% endblock %}
        </main>
      </div>

    </div>
    <!-- /#page-content-wrapper -->
  </div>
  <!-- /#wrapper -->

  <!-- Bootstrap core JavaScript -->
  <script src="{% static 'vendor/jquery/jquery.min.js' %}"></script>
  <script src="{% static 'vendor/bootstrap/js/bootstrap.bundle.min.js' %}"></script>

  <!-- Menu Toggle Script -->
  <script>
    $("#menu-toggle").click(function (e) {
      e.preventDefault();
      $("#wrapper").toggleClass("toggled");
    });
  </script>
</body>

</html>